import cinema.{Cinema}
import scala.util.matching.Regex



/*
@main def main = {
   
  val r = "([a-zA-ZąĄęĘóÓćĆńŃłŁśŚźŹżŻ]{3,20}) ([a-zA-ZąĄęĘóÓćĆńŃłŁśŚźŹżŻ]{3,20})(-[a-zA-ZąĄęĘóÓćĆńŃłŁśŚźŹżŻ]{3,20}){0,1}".r 
  val kino = new Cinema(9)

  println(r.pattern.matcher("ManifolźĘaąd").matches)
  //println(kino.getScreenings(12, 14, 15).toString)
 
  val booking = List((0, List(("tosia", 1), ("zosia", 2), ("kasia", 3))))  
  kino.makeReservation(1, 1, 1, booking) 
  //println(kino.getFreePlaces(1, List((1, 1))))
  //println(kino.isReservationValid(1, 1, 1, booking))

}
*/
